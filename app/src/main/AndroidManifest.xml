<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xiaoyunchengzhu.jniopencvdemo">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".module.base.SharpenActivity" />
        <activity android:name=".module.base.PlusActivity" />
        <activity android:name=".module.base.LightnessActivity" />
        <activity android:name=".module.imgproc.SmoothingActivity" />
        <activity android:name=".module.imgproc.ErodingAndDilatingActivity" />
        <activity android:name=".ImgBaseActivity" />
        <activity android:name=".ImgProcActivity" />
        <activity android:name=".module.imgproc.MorphologyExActivity"></activity>
    </application>

</manifest>